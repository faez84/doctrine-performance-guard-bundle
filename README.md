# faez84/doctrine-performance-guard-bundle

Detect N+1-like duplicate queries and slow/too-many queries for Doctrine DBAL in Symfony.

## Install
```bash
composer require faez84/doctrine-performance-guard-bundle
